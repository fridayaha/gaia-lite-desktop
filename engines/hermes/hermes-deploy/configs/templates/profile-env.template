# Hermes Agent Profile 环境变量
# Profile: {{PROFILE_NAME}}

# LLM 提供商配置
HERMES_PROVIDER={{PROVIDER}}
HERMES_MODEL={{MODEL}}
HERMES_BASE_URL={{BASE_URL}}
HERMES_API_MODE={{API_MODE}}

# API Key (仅当前 Profile 使用的提供商)
{{API_KEY_LINE}}

# 技能预装
HERMES_PRELOAD_SKILLS={{SKILLS}}

# 日志格式
HERMES_LOG_FORMAT=json

# 禁止 Dashboard (由默认 Profile 统一管理)
HERMES_DASHBOARD=0
