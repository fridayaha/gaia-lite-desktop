/**
 * 全站术语常量。HCI 依据：尼尔森「一致性」——新增/添加/确定/确认全站不混用。
 *
 * 约定：
 * - 新增 = 创建业务实体（本体、对象类型、关系、动作）
 * - 添加 = 接入外部资源（数据源、数据集、同步任务）
 */

export const TERMS = {
  /** 业务实体创建动作。 */
  create: '新增',
  /** 外部资源接入动作。 */
  add: '添加',
  /** 主确认按钮。 */
  confirm: '确定',
  /** 取消按钮。 */
  cancel: '取消',
  /** 删除按钮。 */
  delete: '删除',
  /** 编辑按钮。 */
  edit: '编辑',
  /** 保存按钮。 */
  save: '保存',
} as const;

/** 新建业务对象的按钮文案前缀。 */
export const CREATE_PREFIX = '+ 新增';
/** 添加外部资源的按钮文案前缀。 */
export const ADD_PREFIX = '+ 添加';
