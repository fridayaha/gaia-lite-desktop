# 2026-06-11 开发日志

## IM 渠道架构全面优化

### 新增渠道
- [x] 钉钉 (DingTalk) 适配器 — 签名验证、Challenge、消息收发、5000 字符限制

### 流式输出（飞书）
- [x] 飞书卡片流式输出：引擎 SSE → 每 500ms PATCH 更新卡片内容
- [x] 冷/热启动分离：
  - 冷启动：启动状态卡 (`🤖 正在启动...` → `✅ 引擎已就绪`) + 独立回复卡
  - 热启动：仅回复卡，无启动卡，无状态提示
- [x] 卡片双元素架构：流式更新时保留状态指示（冷启动） + 回复内容
- [x] 最终收尾：`replace_with_response` 清理为仅回复内容

### 消息调度优化
- [x] Session 创建兼容 201 状态码（引擎 API 返回 201 而非 200）
- [x] 消息去重：`(agent_id, platform_message_id)` + 60s TTL
- [x] 引擎转发失败重试：503 指数退避 (1s→2s→4s, 最多 3 次)
- [x] 会话缓存 TTL：30 分钟过期自动重建
- [x] 引擎重启检测：`ensure_engine_ready` 返回 `(ready, was_already_running)`
- [x] 配置查询缓存：60s TTL 减少 DB 查询
- [x] 日志修复：k8s 容器 `logging.StreamHandler(stderr)` 配置

### 架构改进
- [x] GET URL 验证通用化（`verify_url` 纳入基类，router 不再写死 wecom）
- [x] 消息长度平台约束截断（`_truncate`）
- [x] 重构 `_patch_card` 通用 PATCH 方法

### Bug 修复
- [x] Session API 返回 201 不被识别的问题（只检查 `==200`）
- [x] 飞书 PATCH 编辑只支持卡片消息（text 消息返回 230001）
- [x] k8s 容器无日志输出（`logger.exception` 被吞掉）
- [x] 热启动时流式更新不应显示状态提示

### E2E 测试发现
- 引擎 Session API 返回 `201 Created`，非 200
- 飞书 PATCH API 仅支持编辑卡片消息（`msg_type=interactive`），不支持纯文本
- k8s Python 容器默认无 logging handlers，所有 `logger.*` 调用不输出
