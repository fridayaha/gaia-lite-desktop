# 本地开发个性化配置（复制为 .env.local 使用，.env.local 已 gitignore）
#
# 默认走真实后端（vite proxy → manager/gateway/hub）。
# 若后端未就绪需前端独立联调，置 true 开启内置 mock（build/mockApi.ts）：
#   内存态覆盖 login + 三层 CRUD + deploy + deploy/events(SSE)，按 01-接口契约.md §2 形状返回。
VITE_USE_MOCK = false
