# 本地开发个性化配置（复制为 .env.local 使用，.env.local 已 gitignore）
#
# 默认走真实后端（vite proxy → manager/gateway/hub）。
# 若后端未就绪需前端独立联调，置 true 开启内置 mock（mockApi.ts）：
#   内存态覆盖 login + accessible 实例 + deploy/events(SSE) + gateway chat(SSE)/sessions/models/files。
VITE_USE_MOCK = false
