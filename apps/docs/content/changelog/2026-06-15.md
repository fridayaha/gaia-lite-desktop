# 2026-06-15 开发日志

## V2 多 Profile 隔离功能

### 核心功能
- [x] V2 引擎镜像 `Dockerfile.v2` + `entrypoint-v2.sh`：nginx + 多 Hermes gateway 进程
- [x] `entrypoint-v2.sh` 权限修复：`chown hermes:hermes` 解决 emptyDir 挂载点权限问题
- [x] Controller `ensure_profile` API（幂等，支持 Pod 调度 + 自动扩 Pod）
- [x] Pod 调度：`_get_instance_pods` / `_select_pod_by_load` / `_ensure_pod_exists`
- [x] nginx 配置动态更新：`update_nginx_config()` 通过 base64 + Python 脚本写入
- [x] Profile 动态创建：`hermes profile create --clone-from base` + gateway 异步启动
- [x] port_map 持久化（修复 SQLAlchemy JSON 列不追踪原地修改的坑）

### IM 用户映射
- [x] `im_user_bindings` 表：企微用户 ID → 平台用户 UUID
- [x] `profile_resolver._resolve_im_user()` 查询映射
- [x] `ResolvedTarget.resolved_user_id` 字段
- [x] 已绑定 LiaoQiWang(admin) / GuoRan(staff_a) / MengLiang(staff_b)

### Gateway 路由
- [x] `_resolve_profile()` 解析 Profile → 调 Controller → 传 `X-Hermes-Profile` 头
- [x] 确定性 Session ID（SHA256，跨重启不变）
- [x] ensure_profile 失败时降级 V1 模式（不中断服务）

### 企业微信渠道测试
- [x] 店长助理（V1）：模型名为空修复（entrypoint 读 `model` 而非 `model_name`）
- [x] 门店助手（V2）：完整多 Profile 隔离验证通过（3 用户 x 3 独立 gateway）

### Bug 修复
- [x] entrypoint `model` 字段名不匹配（`model_name` vs `model`）
- [x] SQLAlchemy JSON 列原地修改不触发脏检测（改为创建新 dict 引用）
- [x] gateway 健康检查超时导致 port_map 不保存（改为异步启动）
- [x] Profile 目录被 root 占用导致 gateway 无法写入 logs
- [x] UA_API_SERVER_KEY 缺失导致 profile .env 写入默认 key
- [x] Controller upsert 唯一键冲突

### 待办
- [ ] 管理后台 IM 渠道绑定界面
- [ ] Profile 动态销毁 / 端口回收
- [ ] 超 max_profiles_per_pod 时自动扩 Pod
- [ ] Pod 重启后 Profile 重建
- [ ] UA_API_SERVER_KEY 注入 Controller 部署
