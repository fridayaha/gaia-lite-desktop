# 2026-06-05 开发日志

## 完成工作

### 基础设施
- [x] Monorepo 初始化（pnpm workspace + pyproject.toml）
- [x] k8s 命名空间 `unionagents` 创建
- [x] PostgreSQL 16 StatefulSet 部署
- [x] MinIO StatefulSet 部署
- [x] infra Secret 配置
- [x] Makefile 命令整理

### Manager 后端
- [x] FastAPI 应用框架搭建
- [x] 数据库 ORM 模型（User, Role, Permission, Agent）
- [x] JWT 认证（access + refresh token）
- [x] RBAC 权限系统（三个预设角色）
- [x] Agent CRUD API
- [x] 种子数据脚本（admin/admin123）
- [x] Dockerfile + k3s 部署

### Engine-Hermes 容器
- [x] Hermes Agent v0.15.2 API Server 模式容器化
- [x] entrypoint.sh — 自动配置 Provider/Model/API Key
- [x] Profile 创建 + 模型配置命令
- [x] DeepSeek v4 Flash 集成
- [x] 流式 + 非流式响应支持
- [x] Dockerfile + k3s 部署

### Gateway
- [x] 纯 HTTP 反向代理实现（不修改 body/query）
- [x] SSE 流式透传
- [x] Authorization header 替换
- [x] ReadError 优雅处理
- [x] k3s 部署

### EndUser (hermes-webui)
- [x] 官方 ghcr.io/nesquena/hermes-webui 镜像部署
- [x] Gateway 透传模式配置（HERMES_WEBUI_CHAT_BACKEND=gateway）
- [x] k3s 部署 + 端口转发

## 端到端验证
- [x] Manager API 登录/CRUD 正常工作
- [x] Gateway → Engine → DeepSeek 非流式对话通过
- [x] Gateway → Engine → DeepSeek 流式对话通过
- [x] hermes-webui 全链路对话通过

## 关键修复
- Python 三引号字符串中 `\\n` 转义问题（导致 JS 语法错误）
- Hermes Profile 模型配置缺失（profile 级别配置覆盖全局 config.yaml）
- Gateway `async with` 作用域问题（StreamingResponse 返回后 httpx client 关闭）
- colima 内存不足导致 Pod 调度失败

## 遗留问题
- Admin 前端未构建部署
- Pod Manager 引擎生命周期未自动化
- Controller 未对接
- 动态引擎路由未实现

## 技术栈确认
- Python 3.11+ / FastAPI / SQLAlchemy async
- Hermes Agent v0.15.2
- DeepSeek v4 Flash
- k3s (colima) / PostgreSQL 16 / MinIO
- hermes-webui (Gateway 透传模式)
