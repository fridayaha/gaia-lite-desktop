# 2026-07-03 开发日志（0.8.36 ~ 0.8.41）

> 本轮覆盖 0.8.36 ~ 0.8.41，主线：worker 稳定性修复、多租户 profile 误取修复、Hermes 引擎容器权限修复、终端门户体验大改、Dify 外接模式用量反查。

## 0.8.41 — Dify 外接模式用量反查

### 可观测性
- [x] `dify_usage_collector`：从 Langfuse 按 `metadata[app_id]` 反查 per-app token 用量 + 成本（查 LiteLLM 价格表算 USD，按 `spend_usd_to_cny` 折 CNY）
- [x] migration 006：`engine_configs` 加 `langfuse_host` / `langfuse_public_key` / `langfuse_secret_key_encrypted`（Fernet 加密存，与 `admin_password_encrypted` 同机制）
- [x] 单测 `test_dify_usage_collector.py`（927 行）

## 0.8.39 — 终端门户体验 + 多租户修复

### 多租户正确性
- [x] `resolve_user_profile` 精确按 `user_id` 解析 profile，避免误取同组他人 profile（多租户隔离修复，根因：`_ensure_profile` 给每条 profile 都同时写 user_id 和 group_id，旧查询 `or_(user_id==?, group_id==instance.group_id)` 会误匹配）

### 终端门户（enduser）体验
- [x] 终端 web 通道优化：chat 内切智能体 / 会话超期跳登录 / 工作区只读浏览
- [x] 工作区目录树改用递归展开模式（移植自 hermes-webui）
- [x] 工作区目录树换用 Lucide SVG 图标（移植自 hermes-webui）
- [x] SSE 自动重连 + 失败消息重试按钮（T5）
- [x] 消息反馈 👍👎 前端 mock（T6）
- [x] 代码块复制按钮（T4）
- [x] 审批卡/思考卡样式对齐 hermes-webui + 思考卡完成后折叠 + 消息耗时/token 展示

### Bug 修复
- [x] engine entrypoint chmod 755 /opt/data 移到 plugins disable 后
- [x] admin nginx location / 加 `Cache-Control no-store`，修重新部署后白屏
- [x] uv.lock 版本号同步 0.8.10→0.8.34

## 0.8.38 — 引擎容器权限
- [x] engine Dockerfile chmod o+rX site-packages，修 per-profile UID 读不了 hermes_cli

## 0.8.37 — worker 连接池死锁
- [x] worker 连接池死锁修复：长 IO 不阻塞 event loop + MinIO 超时 + 调大连接池
- [x] 单测 `test_database_pool.py`

## 0.8.36 — worker finalizer + ASR
- [x] worker `remove_finalizer` 用 null 删除 finalizers 字段
- [x] 豆包 ASR 改走 OpenSpeech submit+query（X-Api-Key 单字段）
