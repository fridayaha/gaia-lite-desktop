# 2026-07-25 开发日志（0.9.0 — 技能工作室）

> 0.9.0 主线：技能工作室（Skill Studio）——在线技能开发/调试/发布一体化环境。从能力中心「开发技能」进入，浏览器内创建技能、与开发引擎对话生成文件、实时调试、校验打包、一键发布到 Hub。

## 技能工作室（Skill Studio）

### 在线开发环境
- [x] 三栏工作台：左栏技能配置（manifest/SKILL.md 元数据 + 在线编辑）、中栏对话（开发/调试双 Tab）、右栏文件树 + CodeMirror 代码编辑器
- [x] 入口：能力中心「技能」卡片「开发技能」按钮（不在侧边栏）
- [x] 工作区 CRUD + 文件读写 + 新建/上传文件

### 引擎会话（dev/debug）
- [x] dev 引擎：开发助手技能 + 读写/bash/grep 工具，帮你生成技能文件
- [x] debug 引擎：注入你的 SKILL.md 作技能 + 只读工具，验证技能行为
- [x] 工具调用卡片可视化（write/edit/bash/grep/read/ls 分形态，diff 着色）
- [x] SSE 流式逐字渲染 + 工具卡片按文本时序交错
- [x] reload-debug 热重载：编辑 SKILL.md 后「重载技能」不重建会话即生效
- [x] 终端面板：bash 命令输出聚合视图

### 消息持久化
- [x] per-turn 缓冲持久化（turn_end 存一行，tool_calls 列存有序 parts 保留文本/工具交错）
- [x] 无 Redis 时退化为 PG 直写（历史不丢失）
- [x] 重载页面后历史完整保留（含工具卡片 + 结果）

### 校验 / 打包 / 发布
- [x] validate：SKILL.md frontmatter + manifest.json 合规校验
- [x] build-package：zip 打包下载
- [x] publish：build → Hub import → 自动安全扫描 → 草稿版本 + hub_item_id 回写
- [x] scan：重扫已发布版本

### 可靠性（Phase 4）
- [x] 崩溃恢复：worker 异常退出自动 respawn（maxRestarts 预算）+ SSE 订阅者转移实现无缝恢复
- [x] 空闲 GC：30min 无活动自动 stop
- [x] 全局并发上限（maxInstances）

### 安全（Phase 5）
- [x] LLM key 运行时从 manager 内部接口拉取（不落 pod env）
- [x] debug skill 注入：SKILL.md 镜像到 .pi/skills/ 供引擎加载

### 部署（Phase 6）
- [x] skill-engine 入云端发版清单（deploy/ci/deployment.yaml）
- [x] Makefile docker-skill-engine + deploy.sh 建 skill_engine DB
- [x] 版本 bump 0.8.153 → 0.9.0

## 附带修复
- [x] DeepSeek 模型名失效（deepseek-chat → deepseek-v4-flash）
- [x] 详情页聊天区高度固定（grid 行高 + viewport calc）
