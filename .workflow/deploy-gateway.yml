version: '1.0'
name: deploy-gateway
displayName: 部署_Gateway_服务
triggers:
  trigger: manual
variables:
  VERSION: "0.5.3"
stages:
  - name: deploy
    displayName: 部署_Gateway
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: deploy@k8s
        name: deploy_gateway
        displayName: K8S部署_Gateway
        version: v1.31.0
        certificate: ${GITCODE_K8S_CERT_ID}
        namespace: unionagents
        yaml: ./deploy/ci/deploy-gateway.yaml
        isReplace: false
        skiptls: false
        params:
          VERSION: 0.5.3
          REGISTRY: ${REGISTRY}
        notify: []
        strategy:
          retry: '0'
